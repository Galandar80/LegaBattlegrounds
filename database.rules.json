{
  "rules": {
    ".read": true,
    "statistiche": {
      ".write": "auth != null && auth.token.admin === true"
    },
    "tornei": {
      ".write": "auth != null && auth.token.admin === true"
    },
    "users": {
      "$uid": {
        ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true)",
        ".validate": "newData.hasChildren(['firstName', 'lastName', 'nickname', 'displayName', 'email', 'updatedAt']) && newData.child('firstName').isString() && newData.child('lastName').isString() && newData.child('nickname').isString() && newData.child('displayName').isString() && newData.child('email').isString() && newData.child('updatedAt').isString()"
      }
    },
    "iscrizioni": {
      "$torneoId": {
        "$uid": {
          ".write": "auth != null && (auth.token.admin === true || (auth.uid === $uid && (!newData.exists() || (root.child('tornei').child($torneoId).exists() && root.child('tornei').child($torneoId).child('iscrizioneAperta').val() !== false))))",
          ".validate": "newData.hasChildren(['firstName', 'lastName', 'nickname', 'displayName', 'email', 'status', 'createdAt']) && newData.child('firstName').isString() && newData.child('lastName').isString() && newData.child('nickname').isString() && newData.child('displayName').isString() && newData.child('email').isString() && newData.child('createdAt').isString() && (auth.token.admin === true || newData.child('status').val() === 'confirmed')"
        }
      }
    },
    "eventi": {
      ".write": "auth != null && auth.token.admin === true"
    },
    "cups": {
      ".write": "auth != null && auth.token.admin === true"
    },
    "hallOfFame": {
      ".write": "auth != null && auth.token.admin === true"
    }
  }
}
